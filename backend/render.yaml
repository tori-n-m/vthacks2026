services:
  - type: web
    name: backend
    env: python
    plan: starter
    buildCommand: pip install -r backend/summarize/requirements.txt
    startCommand: gunicorn backend.wsgi:application
    autoDeploy: true